## 1. 主题与共享视觉基础

- [x] 1.1 新增轻量 `design-system` 共享模块，并在其中建立围绕 `primary #e53935`、`secondary #616161`、`tertiary #0087a0`、`neutral #f9f9fb` 的默认皮肤 brand palette 与 light / dark 主题映射
- [x] 1.2 在共享主题层整理首页、搜索页、播放器与播放列表可复用的 semantic tokens 或局部视觉 token，统一圆角、浅底容器、弱分隔、主强调色和次级文字层级
- [x] 1.3 让 `app` 与 `feature-search` 的主题 wrapper 共同复用共享主题 contract，并明确共享视觉 token 在首页搜索框、搜索结果类型栏、播放列表高亮项、播放器次级容器与 minibar 进度线中的用法，避免各页面继续直接拼接临时色值

## 2. 首页发现流与主壳层样式升级

- [x] 2.1 调整 `feature/main` 中首页搜索入口的样式，使其呈现轻量圆角搜索框、稳定安全区留白，并与独立搜索页保持统一视觉语言
- [x] 2.2 重构首页 banner 推荐位、快捷入口与推荐歌单区的视觉层级，使其符合参考图中的音乐首页风格，同时保持既有发现数据结构与点击行为不变
- [x] 2.3 调整首页纵向歌曲推荐列表的行样式，统一封面尺寸、标题/副标题层级与右侧更多操作入口的排版节奏
- [x] 2.4 调整主壳层底部双 Tab 的容器宽度、圆角、阴影与选中态表现，保留 `首页` / `我的` 双入口，不提前扩成更多一级入口

## 3. 首页 minibar 与 Home 域播放器入口统一

- [x] 3.1 重构首页底部 minibar 的胶囊式容器样式，统一封面、双行信息、主播放按钮与播放列表入口的视觉层级
- [x] 3.2 在不改变主体点击、按钮独立响应和横滑切歌语义的前提下，为 minibar 增加细进度线和更稳定的控件间距
- [x] 3.3 校准 minibar 与首页搜索框、发现流卡片及底部双 Tab 的整体视觉关系，确保 Home 域首页首屏不出现层级冲突或拥挤感

## 4. 搜索页与搜索结果页样式升级

- [x] 4.1 调整搜索页空查询态顶部结构，加入独立返回键，并将搜索输入容器改为轻量圆角搜索栏
- [x] 4.2 将历史搜索区域改造为轻量标签样式，并保留区域级 `Clear all` 与单项移除入口的可识别性
- [x] 4.3 将热门搜索区域改造为统一榜单式容器，稳定列表行高、留白与分隔节奏
- [x] 4.4 调整搜索结果页顶部搜索头和结果类型切换栏的样式，保持固定高度、稳定选中态和主强调指示线
- [x] 4.5 调整搜索结果列表的封面、标题/副标题和右侧更多操作入口样式，形成更紧凑的音乐结果行层级，同时不改变现有搜索数据流与详情跳转行为
- [x] 4.6 修复搜索结果页快速切换结果类型时的空白窗口，确保当前 tab 在加载中至少展示局部 loading，并稳定 pager 与结果状态同步

## 5. 播放器展开页与播放列表样式升级

- [x] 5.1 调整播放器展开页前景层级，使主操作、激活态、辅助信息与次级容器统一到新的共享主题 token
- [x] 5.2 调整播放列表半浮层头部样式，形成包含拖拽指示条、标题、来源信息、条目数量与清空操作的 `Next Up` 风格头部
- [x] 5.3 调整播放列表条目的当前项高亮与普通项样式，统一封面、文本和拖拽手柄的排版基线，同时保持现有排序与点击播放语义不变
- [x] 5.4 校准播放器展开页、播放列表半浮层与首页 minibar 之间的强调色和次级信息层级，确保同一播放上下文在不同页面之间视觉表达一致

## 6. 验证与回归

- [x] 6.1 补充或更新主题、首页、搜索页、播放器和播放列表相关的布局/UI 验证，覆盖主题 token 落地、首页壳层、搜索头部、结果类型栏、minibar 与播放列表高亮项的关键样式约束
- [x] 6.1a 补充搜索结果页快速切换类型的回归验证，覆盖 `SearchViewModel` 中间态与结果页对缺失当前 type state 的 loading 兜底
- [x] 6.2 运行 `./gradlew :playback-service:testDebugUnitTest`、`./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug`，确认本次样式改造未破坏现有播放、搜索和首页能力
- [ ] 6.3 结合 `openspec/changes/refresh-overall-ui-style/references/assets/` 中的本地参考图进行人工对照，确认首页、搜索页、播放器与播放列表的关键视觉方向已经对齐
