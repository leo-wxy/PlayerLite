## 1. 播放列表状态与基础能力

- [x] 1.1 定义 `PlaylistState` 与媒体项的最小必要字段（顺序、激活索引、版本化持久化结构）。
- [x] 1.2 实现播放列表核心操作：新增、删除、重排、设置激活项。
- [x] 1.3 将现有单条目播放入口适配为“单元素播放列表”路径，统一状态迁移。

## 2. 播放流程集成

- [x] 2.1 将播放开始逻辑切换为基于当前激活项驱动。
- [x] 2.2 实现上一首/下一首索引迁移逻辑，并处理首尾边界不越界。
- [x] 2.3 在删除或重排后维护激活索引一致性，避免指向无效条目。

## 3. 持久化与恢复

- [x] 3.1 实现 `playlist_state_v1` 的序列化与本地存储写入（含轻量防抖）。
- [x] 3.2 在启动阶段实现播放列表状态读取、解析和结构校验。
- [x] 3.3 实现恢复时无效条目过滤、激活索引修正与异常回退到空播放列表。
- [x] 3.4 在可用生命周期节点补充持久化 flush，降低异常退出丢失风险。

## 4. 测试与回归验证

- [x] 4.1 增加播放列表增删改排及激活项播放行为测试。
- [x] 4.2 增加下一首/上一首边界与单条目入口兼容测试。
- [x] 4.3 增加持久化恢复、损坏数据回退、版本不兼容场景测试。
- [x] 4.4 运行相关测试与回归检查，修复与本变更相关的失败项。

## 5. 播放列表 UI 交互

- [x] 5.1 在主界面右下角新增播放列表按钮，并显示播放项数量角标。
- [x] 5.2 实现点击按钮后从底部上滑展开的半浮层 bottom sheet（含遮罩与关闭入口）。
- [x] 5.3 在半浮层内实现播放项高亮、点击切换、删除播放项与播放状态衔接。

## 6. 上一首/下一首快捷切换

- [x] 6.1 在主界面播放控制区新增“上一首/下一首”按钮，并根据边界状态禁用。
- [x] 6.2 实现上一首/下一首切换逻辑（更新激活项、准备音频、播放态下连续切换）。
- [x] 6.3 验证首尾边界、暂停态与播放态切换行为，确保状态提示正确。
- [x] 6.4 调整“上一首/下一首”与“播放/暂停”按钮间距，改善触控可用性与视觉分隔。

## 7. 播放列表拖拽排序与播控精简

- [x] 7.1 在播放列表半浮层实现长按拖拽排序交互，并实时更新列表顺序。
- [x] 7.2 拖拽排序后保持激活项语义一致，验证播放态/暂停态下状态连续性。
- [x] 7.3 移除主界面独立 stop 按钮，保留上一首、播放/暂停、下一首核心操作。
- [x] 7.4 回归验证播放控制区布局、列表拖拽重排与切歌行为协同正常。
