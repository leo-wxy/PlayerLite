## 1. 搜索数据层与状态模型

- [x] 1.1 将热搜接口切换为 `/search/hot/detail`，扩展热搜 mapper / UI model，使其能够承接更丰富的推荐热搜字段
- [x] 1.2 在 `SearchRepository` 中补齐本地持久化的历史搜索读写、去重和数量限制，并继续保持搜索请求在有登录态时自动透传 cookie / csrf
- [x] 1.3 调整 `SearchUiState` / `SearchViewModel`，让空查询态同时承接“历史搜索 + 推荐热搜”，并在提交搜索、点击建议词、点击热搜词后更新历史记录
- [x] 1.4 保留热搜缓存策略，确保搜索页重进时优先复用可用推荐热搜数据而不是频繁重复请求

## 2. 独立搜索页承载

- [x] 2.1 将搜索页顶部继续上提，改成更贴近状态栏的单行紧凑搜索栏，进一步减少顶部留白
- [x] 2.2 将空查询态改成“固定历史搜索区 + 下方独立滚动推荐热搜区”，确保历史区不随推荐热搜滚动而移动
- [x] 2.3 调整建议态与结果态，在这些状态下隐藏固定历史搜索区，并让可滚动列表占据主要空间
- [x] 2.4 将搜索页视觉收敛为中度网易云风格，统一顶部搜索栏、历史搜索标签、推荐热搜榜单层次与结果列表密度
- [x] 2.5 将首页概览态整体视觉收敛为中度网易云风格，统一顶部搜索入口、发现区标题、banner、横滑推荐卡和快捷入口卡的设计语言
- [x] 2.6 调整首页底部播放入口卡，使其与首页概览态和搜索页保持一致的音乐 App 风格
- [x] 2.7 统一放大搜索页字体层级，提升输入、历史、热搜、建议与结果区域的可读性
- [x] 2.8 移除首页快捷入口卡中的重复“推荐”标签，并适度降低该类卡片高度

## 3. 首页搜索入口接线

- [x] 3.1 在 `AndroidManifest.xml` 注册 `SearchActivity`，并在 `AppContainer` 中补充搜索仓库的构建与注入入口
- [x] 3.2 将首页悬浮搜索框点击行为从 Toast 占位改为由主壳层启动 `SearchActivity`，保持 `HomeOverviewScreen` 继续只通过回调暴露点击事件
- [x] 3.3 清理首页旧的占位搜索点击处理，确保首页默认关键词轮播逻辑与新搜索页导航逻辑彼此独立

## 4. 测试与回归验证

- [x] 4.1 为搜索相关 repository / storage 补充单元测试，覆盖 `/search/hot/detail` 映射、热搜缓存命中/未命中、历史搜索持久化与 cookie 自动透传约束
- [x] 4.2 为 `SearchViewModel` 补充状态流转测试，覆盖空查询态历史搜索展示、提交后写入历史、清空查询回退历史+热搜
- [x] 4.3 补充搜索页 UI/交互验证，覆盖更贴顶的搜索栏、固定历史搜索区、独立滚动推荐热搜区，以及建议/结果态隐藏历史区的行为，并按仓库要求重新运行 `./gradlew :app:testDebugUnitTest`、`./gradlew :playback-service:testDebugUnitTest`、`./gradlew :app:assembleDebug`
- [x] 4.4 为搜索页补充视觉层次回归验证，覆盖轻量历史标签、榜单式推荐热搜样式结构与更紧凑的结果列表承载
- [x] 4.5 为首页概览态补充视觉回归验证，覆盖顶部搜索入口、推荐区块样式与底部播放入口卡的结构断言
- [x] 4.6 为搜索页与首页快捷入口补充细化回归验证，覆盖搜索页放大字体与快捷入口去标签、降高度的结构约束
