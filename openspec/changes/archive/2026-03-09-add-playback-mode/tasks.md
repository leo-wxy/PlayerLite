## 1. 共享播放模式契约与播放列表持久化

- [x] 1.1 新增共享的 `PlaybackMode` 模型，以及用于模式切换和快照透传的 `MediaSession` command / extra 常量。
- [x] 1.2 扩展远端快照解析与前台播放状态模型，使其能够从 service extras 中携带当前播放模式。
- [x] 1.3 将播放列表持久化从单一的 `items + activeIndex` 升级为原始顺序、随机顺序、激活项 id、播放模式和 `showOriginalOrderInShuffle`，并兼容已有存量状态迁移。
- [x] 1.4 更新播放列表 / 会话协调层，基于双顺序状态派生活动索引与展示列表，并保证模式切换时当前播放项不丢失。

## 2. 播放器 UI 与播放列表面板行为

- [x] 2.1 将主控区左侧的倍速按钮替换为播放模式入口，以图标展示当前模式，并按“列表 → 单曲 → 随机”直接循环切换且显示 Toast 提示。
- [x] 2.2 将播放模式切换串到 `PlayerViewModel`、`PlayerRuntime` 与 `PlayerServiceBridge`，保证模式变化时当前播放项保持不变。
- [x] 2.3 更新播放列表 bottom sheet：随机模式下默认展示随机顺序，增加 `显示原始顺序` 复选框，并将该状态持久化到应用重启后仍可恢复。
- [x] 2.4 在当前展示为随机顺序时禁用拖拽重排，同时保留原始顺序视图和非随机模式下的重排能力。

## 3. 后台播放执行链路与会话联动

- [x] 3.1 扩展 `PlaybackProcessState` 与 `PlayerMediaSessionService`，让会话状态能够透出当前播放模式相关投影信息，并支持模式切换所需的同步入口。
- [x] 3.2 实现列表循环、单曲循环与随机播放下的上一首、下一首和自然播完导航规则。
- [x] 3.3 保证只有在切入随机模式时才生成或刷新随机顺序；点击列表项和后续导航都复用当前随机顺序；切出随机模式时保持当前项稳定。
- [x] 3.4 保证 service 侧播放队列投影、当前媒体 id 与播完后的衔接行为与实际生效顺序保持一致，使前台 UI 和外部媒体控制保持同步。

## 4. 验证与回归覆盖

- [x] 4.1 为播放列表状态迁移、随机顺序持久化和 `showOriginalOrderInShuffle` 重启恢复补充单元测试。
- [x] 4.2 为模式切换、模式化上一首 / 下一首、不同行为模式下的自然播完，以及随机模式点击列表项后顺序保持稳定补充运行时或 service 测试。
- [x] 4.3 为播放模式入口、随机模式专属复选框可见性，以及随机顺序视图下禁用拖拽补充 UI 向测试或针对性检查。
- [x] 4.4 运行受影响模块的聚焦验证，并补充模式切换、随机展示和重启恢复相关的手动检查说明。
