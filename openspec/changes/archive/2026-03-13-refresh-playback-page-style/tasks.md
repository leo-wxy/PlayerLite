## 1. 播放页骨架收敛

- [x] 1.1 重构 `app/src/main/java/com/wxy/playerlite/feature/player/ui/PlayerScreen.kt` 的整体布局为“主视觉区 / 进度区 / 播控区”三段式骨架
- [x] 1.2 移除播放页主界面的调试型浮动按钮与音频统计卡，确保标准播放态不再展示 `UI Test`、`清缓存`、登录浮动入口和 `Audio Info`
- [x] 1.3 保持 `PlayerExpandedScreen` 现有返回壳层不回退，并确认 `PlayerScreen` 不再重复承载额外导航层

## 2. 播放信息与进度层级

- [x] 2.1 调整播放页主视觉承载，仅保留封面 / 唱片主视觉与轻量播放状态，不再叠加重复百分比与冗余状态文案
- [x] 2.2 在进度条上方按“歌曲名在上、歌手名在下”的顺序展示当前播放信息，并保留当前时长 / 总时长
- [x] 2.3 梳理空状态与已选中播放状态的分支布局，保证空状态仅展示轻量引导与单一“选择音频”入口

## 3. 核心播控与播放列表入口

- [x] 3.1 调整 `PlaybackControls` 或其调用布局，保留上一首 / 播放暂停 / 下一首三类主操作，并统一成更偏云音乐风格的主控层级
- [x] 3.2 保留播放模式入口与播放列表入口，但将它们收敛为与主控区同层的次级控制位，不再继续依赖右下角独立悬浮入口
- [x] 3.3 验证 `PlaylistBottomSheet`、播放模式切换、进度拖动与现有播放状态回调在新布局下行为不回退
- [x] 3.4 在播放页右上角增加歌曲百科入口 icon，仅对具备真实在线 `songId` 的播放内容展示
- [x] 3.5 接入歌曲简要百科接口与轻量信息面板，覆盖加载中、成功摘要、空摘要与失败重试状态

## 4. 回归验证

- [x] 4.1 补充播放页 UI / 布局验证，覆盖简洁播放态、空状态、歌曲名 / 歌手名位于进度条上方，以及播放列表入口仍可达
- [x] 4.2 补充必要的组件或状态测试，覆盖移除调试信息后核心播控与空状态分支仍然稳定
- [x] 4.3 按仓库要求运行 `./gradlew :playback-service:testDebugUnitTest`、`./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug`，确认播放页风格重整未破坏现有构建
- [x] 4.4 补充播放页百科入口与摘要面板验证，覆盖“在线歌曲展示入口 / 本地音频不展示 / 点击后展示摘要状态”
