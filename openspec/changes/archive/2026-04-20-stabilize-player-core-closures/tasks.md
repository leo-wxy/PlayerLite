## 1. 播放闭环

- [x] 1.1 补充播放目标 generation 测试，覆盖旧 source completion、重复 prepare 和切歌回跳不能覆盖当前曲目
- [x] 1.2 在 app 侧播放编排、播放服务运行时和 native 回调桥接中收口当前播放目标 generation
- [x] 1.3 补充服务重建和控制器重连测试，覆盖恢复队列、当前项和播放位置时不被临时失败清空
- [x] 1.4 实现缓冲失败的有限重试、最终失败态和远端快照投影
- [x] 1.5 补充弱网恢复定向测试，覆盖短时空读、连接中断后继续当前项
- [x] 1.6 补充冷启恢复测试，覆盖 app 与播放服务进程都重建后仍能恢复当前队列项、播放意图和上次进度
- [x] 1.7 实现播放会话持久化与冷启恢复，记录 active item、positionMs、playWhenReady 与保存时间，并在无远端存活快照时优先恢复

## 2. 在线资源闭环

- [x] 2.1 补充在线播放准备结果分类测试，覆盖 URL 过期、资源不可用、试听片段、未授权和短时网络失败
- [x] 2.2 在在线地址解析和 source adapter 层返回统一解析结果分类与过期信息
- [x] 2.3 实现 URL 过期刷新和当前单曲范围的主备 source 兜底
- [x] 2.4 补充搜索结果到共享队列项的可恢复上下文测试，确保无长期直链时仍可重新解析播放资源
- [x] 2.5 补充音质切换测试，覆盖 URL 过期重解析、当前位置重准备和目标音质不可用回退

## 3. 队列与歌单闭环

- [x] 3.1 补充共享队列项 source 上下文测试，覆盖单曲来源 override 可恢复但不污染长期资产
- [x] 3.2 实现当前队列项单曲来源切换，保持队列顺序、active index 和全局当前 source 不变
- [x] 3.3 补充“下一首播放”与长期歌单资产边界测试，确保插队不改写收藏或来源歌单
- [x] 3.4 将本机最近播放写入时机收口为实际成功开播事件，并补充失败点击不记录、重试成功只记录一次的测试

## 4. 验证与回归

- [x] 4.1 运行播放闭环相关定向测试
- [x] 4.2 运行在线资源、source adapter、音质切换相关定向测试
- [x] 4.3 运行队列、最近播放和共享契约相关定向测试
- [x] 4.4 运行默认回归验证：`./gradlew :playback-service:testDebugUnitTest`、`./gradlew :app:testDebugUnitTest`、`./gradlew :app:assembleDebug`
- [x] 4.5 运行冷启恢复相关定向测试，并在实现后重跑默认回归验证
- [x] 4.6 补充真机恢复位置链路诊断，确认 UI 恢复进度、队列同步参数、service 处理分支和 runtime 实际起播位置一致
