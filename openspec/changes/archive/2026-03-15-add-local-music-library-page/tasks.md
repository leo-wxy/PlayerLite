## 1. 入口与导航

- [x] 1.1 在个人中心页增加“本地歌曲”入口，并让登录态/游客态都能进入独立页面
- [x] 1.2 接通本地歌曲页 Activity/Route 与返回导航

## 2. 数据与缓存

- [x] 2.1 新增本地歌曲 Repository，接通 `MediaStore` 扫描并映射本地歌曲行模型
- [x] 2.2 为扫描结果增加本地快照缓存，并让再次进入时优先展示缓存内容
- [x] 2.3 为本地歌曲页补齐权限态、空态、扫描失败态和手动重新扫描入口

## 3. 播放接线

- [x] 3.1 将本地歌曲列表投影到现有外部队列播放入口，支持“播放全部”默认第一首
- [x] 3.2 支持点击某首本地歌曲后按当前列表的目标 index 开始播放，并在播放成功后回到主壳层展开播放器、失败时保留当前页提示用户

## 4. 验证

- [x] 4.1 为入口展示、缓存快显、手动重扫和本地歌曲页播放行为补充 ViewModel / Robolectric 测试
- [x] 4.2 运行 `./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug`
