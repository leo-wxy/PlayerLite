## 1. 详情页播放网关

- [x] 1.1 新增详情页共享播放网关，统一承接歌单、歌手、专辑详情页的“播放全部”和点歌播放
- [x] 1.2 支持以当前详情上下文整体替换当前队列，并在稳定条目序列相同时只切换激活项

## 2. 两段式元数据装配

- [x] 2.1 详情页发起播放时先凭 `songId` 立即建立队列并开始播放
- [x] 2.2 后台分页请求 `/song/detail`，按稳定 item id 异步回填标题、歌手、专辑、封面、稳定 `artistId` 与时长

## 3. 详情页 UI 与动态信息

- [x] 3.1 改造歌单、歌手、专辑详情页 UI / ViewModel，补齐“播放全部”与歌曲点击播放入口，并在成功替换播放列表后自动打开播放器展开页
- [x] 3.2 为歌单详情页补齐动态信息独立加载、失败重试与真实播放后播放量更新
- [x] 3.3 为专辑详情页播放队列补齐专辑封面兜底

## 4. 验证

- [x] 4.1 为详情页播放入口、同上下文激活项切换、异步 enrichment、稳定 `artistId` 回填、动态信息、自动跳播放器与封面兜底补充测试
- [x] 4.2 运行 `./gradlew :playback-service:testDebugUnitTest`、`./gradlew :app:testDebugUnitTest`、`./gradlew :app:assembleDebug`
