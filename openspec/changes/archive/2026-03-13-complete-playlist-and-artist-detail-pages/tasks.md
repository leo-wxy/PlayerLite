## 1. 宿主路由与依赖装配

- [x] 1.1 在 `AppContainer` 中新增歌手详情与歌单详情 repository 的创建、缓存与对外访问入口，继续复用现有 `JsonHttpClient` 和登录态透传链路
- [x] 1.2 在 `PlayerLiteApplication.searchHostDependencies()` 中补齐 `SearchRouteHandler`，让 `SearchRouteTarget.Artist` 与 `SearchRouteTarget.Playlist` 能启动对应详情页并传递稳定实体标识
- [x] 1.3 补齐歌手详情页、歌单详情页的 `Intent` factory 与 `AndroidManifest.xml` 注册，保持未支持的搜索结果类型不被误跳转
- [x] 1.4 为专辑详情补齐 repository 创建入口、`SearchRouteTarget.Album` 宿主分发、`Intent` factory 与 `AndroidManifest.xml` 注册，继续保持未支持结果类型不被误跳转

## 2. 歌手详情页实现

- [x] 2.1 新增歌手详情 remote data source、repository、mapper 与页面模型，同时接入“获取歌手详情”和“歌手热门 50 首歌曲”接口，并映射为稳定的基础信息/热门歌曲承载
- [x] 2.2 新增歌手详情 `ViewModel` 与状态模型，覆盖首屏加载、页级失败、热门歌曲区块级失败重试和信息不足时的兜底展示
- [x] 2.3 实现歌手详情 Activity / Compose Screen，并接入共享的云音乐风格 detail scaffold、头像主色取色渐变头图、圆形歌手主视觉、头像点击放大预览、热门歌曲列表区块，以及简介点击后浮层展开全文
- [x] 2.4 接入“歌手简要百科信息”接口，将百科摘要/段落信息融合到歌手详情页，并保证百科缺失或失败时不阻塞已加载内容

## 3. 歌单详情页实现

- [x] 3.1 新增歌单详情 remote data source、repository、mapper 与页面模型，同时接入“获取歌单详情”和“获取歌单所有歌曲”两个接口
- [x] 3.2 新增歌单详情 `ViewModel` 与状态模型，将歌单头部信息和歌曲列表拆成独立状态区域，并支持列表区块级重试
- [x] 3.3 实现歌单详情 Activity / Compose Screen，接入共享的云音乐风格 detail scaffold、圆角歌单封面、凸显的作者/歌曲数元信息，以及简介点击后浮层展开全文

## 4. 专辑详情页实现

- [x] 4.1 新增专辑详情 remote data source、repository、mapper 与页面模型，同时接入“获取专辑内容”和“专辑动态信息”接口，并映射为稳定的专辑头部/简要百科/歌曲列表/动态元信息承载
- [x] 4.2 新增专辑详情 `ViewModel` 与状态模型，将专辑内容主状态与动态信息副状态拆成独立承载区域，并支持动态区块级重试
- [x] 4.3 实现专辑详情 Activity / Compose Screen，接入共享的云音乐风格 detail scaffold、圆角专辑封面、动态元信息区块、简介点击后浮层展开全文，以及紧凑歌曲列表承载

## 5. 搜索闭环与验证

- [x] 5.1 将搜索结果点击闭环接入宿主详情页导航，补充歌手/歌单结果点击后实体标识透传的一致性回归验证
- [x] 5.2 为歌手详情与歌单详情的数据层、mapper、`ViewModel` 补充单元测试，覆盖成功、失败、重试、歌手热门歌曲区块以及歌单头部/列表分段状态
- [x] 5.3 扩展搜索结果点击闭环到专辑详情页，补充专辑结果点击后实体标识透传的一致性回归验证
- [x] 5.4 为歌手简要百科信息与专辑详情的数据层、mapper、`ViewModel` 补充单元测试，覆盖成功、失败、重试、百科摘要融合、歌曲列表承载与动态信息副状态
- [x] 5.5 补充详情页 UI/结构验证，覆盖共享 detail scaffold、歌手头像取色渐变头图、歌手头像放大预览、歌手百科摘要、歌手/歌单/专辑简介浮层、歌单头部作者/歌曲数元信息，以及专辑百科摘要、动态信息区块与紧凑歌曲列表承载

## 6. 首页、个人中心与详情浮层点击闭环

- [x] 6.1 扩展首页发现流的数据模型与 mapper，为 banner、推荐卡和快捷入口补齐统一内容动作，覆盖站内歌手/歌单/专辑详情、可解析链接与未支持目标的稳定兜底语义
- [x] 6.2 扩展个人中心内容列表的数据模型与 mapper，为收藏歌手、用户歌单和收藏专栏补齐统一内容动作，确保歌手/歌单进入站内详情，专栏项保留可点击反馈与稳定提示
- [x] 6.3 在 `MainActivity` 宿主层补齐首页和个人中心 item 点击处理，复用现有详情页路由与系统链接打开能力，同时让首页/个人中心卡片具备明确点击反馈
- [x] 6.4 将歌手/歌单/专辑简介全文浮层升级为可滚动承载，并把歌单/专辑详情页头图升级为封面主色取色渐变，同时让详情页返回键固定在外层，确保三类详情页的头部交互一致
- [x] 6.5 固定搜索结果页类型筛选 Tab 的 chip 高度，并让文案在 chip 内保持真正居中，避免切换不同结果类型时筛选条抖动或文字偏移
- [x] 6.6 为首页与个人中心的内容动作映射、宿主点击分发、详情全文浮层、歌单/专辑取色头图和搜索结果类型筛选条补充测试，覆盖站内详情、外链/兜底分支、长文本浮层滚动承载、取色渐变兜底与固定高度约束
- [x] 6.7 按仓库要求重新运行 `./gradlew :feature-search:testDebugUnitTest`、`./gradlew :app:testDebugUnitTest`、`./gradlew :playback-service:testDebugUnitTest` 与 `./gradlew :app:assembleDebug`

## 7. 歌单 / 专辑 分页加载优化

- [x] 7.1 将歌单详情 repository / remote data source / `ViewModel` 改为分页加载歌曲列表，优先展示头部与首批曲目，并支持续页加载与尾部失败重试
- [x] 7.2 将专辑详情 repository / remote data source / `ViewModel` 改为分页加载歌曲列表，优先展示头部与首批曲目，并支持续页加载与尾部失败重试
- [x] 7.3 调整歌单详情页与专辑详情页 UI，补齐尾部“加载中 / 加载失败 / 没有更多内容”承载，并在滚动接近底部时触发下一页
- [x] 7.4 为歌单与专辑的分页数据层、状态机和 UI 尾部承载补充测试，覆盖首批成功、续页成功、续页失败重试与已显示内容保留
