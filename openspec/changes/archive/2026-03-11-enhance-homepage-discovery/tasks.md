## 1. 首页发现数据与状态建模

- [x] 1.1 新增首页发现 remote data source、repository 与接口/DTO 映射，支持将“首页-发现”响应转换为稳定的 Home section UI model
- [x] 1.2 在 `AppContainer` 中接入首页发现依赖，并新增独立的 `HomeViewModel`/状态持有者来暴露加载、空态、异常态与最近一次成功结果
- [x] 1.3 实现默认搜索关键词来源策略，优先读取接口返回关键词，缺失时回退到本地兜底关键词列表

## 2. 首页概览态 UI 改造

- [x] 2.1 将 `HomeOverviewScreen` 从静态占位布局替换为可滚动的发现内容容器，并按 section 顺序渲染首页区块
- [x] 2.2 在首页顶部增加悬浮搜索框，处理顶部安全区与列表预留间距，并接入默认关键词轮播展示
- [x] 2.3 将首页底部播放入口调整为独立底部浮层，确保发现内容滚动时仍可稳定进入 `PLAYER_EXPANDED`
- [x] 2.4 将 banner 区块调整为固定高度轮播，并让横向发现区块改用容器级 padding + 固定 item 高度，避免滚动抖动和首卡阴影裁切
- [x] 2.5 将 banner 辅助信息改为纯文字样式，并统一首页 item 标题/副标题最多一行显示
- [x] 2.6 让 banner 图片在卡片内完整铺满，并将 banner 的左右容器 padding 收窄到小于普通横向区块
- [x] 2.7 让 banner 轮播具备真循环体验，连续左右滑动时不出现明显首尾边界
- [x] 2.8 将 banner 下方每日推荐快捷入口改为低高度纯文字卡，并使用稳定生成的浅色背景增强视觉区分
- [x] 2.9 将 banner 后普通推荐卡的封面改为满宽 1:1 显示，去掉图片额外外间距

## 3. Home 域集成与交互收口

- [x] 3.1 在 `MainActivity` / Home 壳层接入首页发现状态收集与事件分发，保持 `MainActivity` 只做轻量 wiring
- [x] 3.2 保持 `HomeSurfaceMode`、底部 Tab 显隐和返回链路不变，确保从播放展开态返回时恢复首页发现上下文而非重新闪空
- [x] 3.3 明确搜索框点击的首版行为，并将其封装为独立回调边界，避免与未来搜索页实现强耦合

## 4. 验证与回归

- [x] 4.1 为首页发现 repository / 状态层补充单元测试，覆盖加载成功、空结果、失败保留旧数据和关键词兜底
- [x] 4.2 为首页概览态补充 UI 或状态验证，覆盖悬浮搜索框、发现区块渲染和底部播放入口触发播放展开态
- [x] 4.3 运行 `./gradlew :app:testDebugUnitTest`、`./gradlew :playback-service:testDebugUnitTest` 与 `./gradlew :app:assembleDebug` 完成回归验证
- [x] 4.4 补充首页布局规格测试，覆盖 banner 轮播、横向区块 content padding、固定卡片高度与单行文案约束
- [x] 4.5 补充 banner 铺满与 banner 专用 padding 规格测试
- [x] 4.6 补充 banner 真循环映射规则测试
- [x] 4.7 补充每日推荐快捷入口规格测试，覆盖无图、低高度与稳定背景色
- [x] 4.8 补充普通推荐卡封面规格测试，覆盖满宽显示与 1:1 比例
