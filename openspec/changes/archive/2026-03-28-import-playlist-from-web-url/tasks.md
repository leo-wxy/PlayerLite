## 1. 导入数据链路与匹配建模

- [x] 1.1 定义网页歌单导入所需的来源类型、导入会话、曲目线索、匹配结果与汇总统计模型，并补齐与现有 `SearchRepository`、`PlaylistItem` 接轨所需的数据映射入口
- [x] 1.2 新增按来源拆分的网页歌单导入适配层与 remote data source，覆盖网易云链接 ID 提取与既有歌单详情复用、QQ 音乐链接 ID 提取与来源专用详情请求、短链展开、不支持来源与解析失败的状态映射
- [x] 1.3 实现来源曲目提取 use case：先把网易云和 QQ 音乐歌单解析为统一的原始曲目结构，并把已具备当前可播放稳定主键的条目标记为 `direct`
- [x] 1.4 为缺少当前可播放主键的条目实现 `ImportTrackQueryBuilder` 与 `ImportTrackMatcher`，按“歌名 + 主歌手”为主查询词、必要时附加专辑信息调用搜索，并基于标题/歌手/专辑/时长输出 `matched`、`ambiguous`、`unmatched`
- [x] 1.5 为匹配链路补齐查询去重、会话内结果缓存、受控并发、登录前置校验与限频暂停策略，确保大歌单不会因瞬时搜索请求放大而触发明显风控

## 2. 导入入口与预览流程

- [x] 2.1 在个人页快捷入口中接入“导入歌单”入口，并补齐独立 `WebPlaylistImportActivity` / `ViewModel` 的依赖装配与导航承载；后续设置页入口仅复用同一导入流程
- [x] 2.2 实现网页歌单导入输入页，覆盖 URL 粘贴、提交、来源解析中、歌单读取中、链接不支持、解析失败、未登录引导与整单读取失败等状态
- [x] 2.3 实现导入预览页，展示歌单来源信息、封面/标题、总曲目数、`direct` 数、匹配成功数、歧义数、未匹配数、当前匹配进度以及逐曲解析/匹配结果
- [x] 2.4 为预览页补齐渐进式更新和失败保留行为：后台匹配继续执行时实时刷新统计；出现限频或连续失败时保留已完成结果并提示“匹配已暂停”
- [x] 2.5 为预览页补齐确认导入按钮的可用性约束，确保没有任何 `direct` 或 `matched` 条目时不能提交导入，并向用户解释原因

## 3. 播放列表接入与导入确认行为

- [x] 3.1 将导入预览中的 `direct` 与 `matched` 条目映射为带稳定 `id`、`contextType/contextId/contextTitle` 的在线 `PlaylistItem`
- [x] 3.2 在用户确认导入时复用现有 `playlist-management` 队列替换链路，用全部可导入条目替换当前播放列表并以首个可导入条目作为激活项
- [x] 3.3 补齐导入成功后打开 `PlayerActivity`、导入失败保持当前页不跳转、没有可用匹配项时保持当前播放上下文不变的行为
- [x] 3.4 校验导入后的当前队列与现有 `playlist-persistence` / `PlayerRuntime` 语义一致，确保后续切歌、删除和恢复不会丢失导入上下文

## 4. 测试与回归验证

- [x] 4.1 补充导入来源适配器 / matcher 的单元测试，覆盖来源识别、链接 ID 提取、解析失败、不支持来源、查询词生成、`direct`/`matched`/`ambiguous`/`unmatched` 分类、重复查询复用和登录前置约束
- [x] 4.2 补充导入输入页与预览页的 ViewModel / UI 测试，覆盖个人页入口导航、统计展示、匹配进度渐进更新、按钮可用性、失败提示和导入确认交互
- [x] 4.3 补充导入结果接入当前播放列表的回归测试，覆盖仅导入 `direct + matched` 条目、空可导入结果不替换队列以及导入后自动打开播放器的行为
- [x] 4.4 按仓库要求运行 `./gradlew :playback-service:testDebugUnitTest`、`./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug`，确认导入链路改动未破坏现有构建与播放流程
