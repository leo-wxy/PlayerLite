## 1. 账户域共享视觉基础

- [x] 1.1 在 `LoginActivity.kt` 与 `MainShellScreen.kt` 间梳理可复用的账户域视觉 token，统一强调色、背景渐变、卡片圆角、阴影和页面间距
- [x] 1.2 抽离登录页与个人页共用的局部展示组件或辅助函数，避免两页分别维护重复的品牌背景、头部容器和主按钮样式

## 2. 登录页 UI 风格升级

- [x] 2.1 重构 `LoginScreen` 页面结构，形成更明确的品牌头部、标题副标题、浮层表单卡和轻量跳过入口层级
- [x] 2.2 调整登录方式切换区、输入框和主登录按钮样式，使其符合新的音乐产品视觉语言，同时保持手机号/邮箱登录流程与交互回调不变
- [x] 2.3 处理登录页在小屏幕和输入法弹出场景下的可达性，确保主视觉、表单区和提交操作不会被遮挡到无法完成登录

## 3. 用户中心页 UI 风格升级

- [x] 3.1 重构 `UserCenterScreen` 的页面骨架，形成头部资料区、基础信息区和核心操作区的个人主页式布局
- [x] 3.2 调整未登录态和已登录态的视觉呈现，使头像、昵称、摘要、登录引导和退出登录按钮在同一页面骨架内完成状态切换
- [x] 3.3 确保用户中心页与登录页保持同源的强调色、卡片层级和品牌化头部表达，同时不改变现有 `UserSessionUiState` 投影与登录入口行为

## 4. 验证与回归

- [x] 4.1 更新或补充 `LoginScreenUiTest`，覆盖品牌化欢迎布局、登录方式切换、主登录按钮和轻量跳过入口仍然可见且可交互
- [x] 4.2 更新或补充 `UserCenterScreenUiTest`，覆盖已登录/未登录两态下的头部资料区、主操作按钮和基础信息区展示
- [x] 4.3 运行 `./gradlew :app:testDebugUnitTest` 与 `./gradlew :playback-service:testDebugUnitTest`，确认本次 UI 风格调整未破坏现有状态与业务测试
- [x] 4.4 运行 `./gradlew :app:compileDebugAndroidTestKotlin` 与 `./gradlew :app:assembleDebug`，确认相关 Compose UI 测试与应用构建通过
