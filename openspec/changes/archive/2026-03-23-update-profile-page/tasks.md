## 1. 用户中心数据源扩展

- [x] 1.1 扩展 `UserCenterRepository` / remote data source，接入最近播放歌曲接口 `/record/recent/song`，并定义最近播放子页面所需的 UI 模型与映射逻辑
  当前 mapper 采用宽松解析：优先覆盖 `data.list[].data.{id,name,ar[],al.{name,picUrl}}`，未覆盖字段后续可按真实响应补齐。
- [x] 1.2 扩展用户歌单映射，区分“用户自己的歌单”和“喜欢入口”所需信息
- [x] 1.3 为“喜欢”入口补齐可复用的目标解析逻辑，优先基于用户歌单数据定位“我喜欢的音乐/喜欢歌单”
- [x] 1.4 扩展 `UserCenterRepository` 接入“喜欢/收藏”子页面所需接口（例如 `/artist/sublist`、`/mv/sublist`、`/topic/sublist`、收藏歌单映射等），并补齐 mapper 单测

## 2. 页面状态与加载编排

- [x] 2.1 扩展 `UserCenterViewModel` 状态模型，使主页只承载快捷入口和“用户自己的歌单”列表
- [x] 2.2 编排主页与最近播放子页面的加载顺序，保证游客态不触发任何受登录态约束的在线请求
- [x] 2.3 保持会话失效统一降级逻辑，让用户歌单与最近播放接口在认证失效时一致回退到未登录态

## 3. 个人页 UI 调整

- [x] 3.1 参考 `cc.html` 重构 `UserCenterProfileHeader`，补强头像主视觉、昵称/会员信息与头部信息层级
- [x] 3.2 在头部资料区下方新增快捷入口区，至少提供“喜欢”“最近”“本地”入口，并保持游客态可见
- [x] 3.3 调整个人首页主体，使其暂时只展示“用户自己的歌单”，并按 UI 样式优化列表节奏
- [x] 3.4 新增最近播放子页面，补齐加载态、空态、错误态与局部重试反馈
- [x] 3.5 接通快捷入口跳转：`本地` 复用 `LocalSongsActivity`，`最近` 进入最近播放子页面，`喜欢` 进入新的“喜欢”子页面（Tab 切换展示收藏内容）
- [x] 3.6 为最近播放歌曲和当前没有落地路由的新内容类型提供明确点击反馈，未支持时返回稳定提示而不是静默无响应
- [x] 3.7 新增“喜欢”子页面：以 n 个 Tab 切换展示对应收藏内容，并补齐加载态、空态、错误态与重试
- [x] 3.8 继续调整主页与“喜欢”子页面的视觉样式，使其更贴近 `cc.html` 的轻量头部、图标入口与行式列表风格

## 4. 集成与回归验证

- [x] 4.1 调整 `MainActivity` / 相关装配代码，确保用户中心页面消费新的组合状态与交互回调
- [x] 4.2 为新增 mapper / repository / viewmodel 行为补充单元测试，覆盖最近播放、主页歌单过滤、喜欢入口解析、未登录不请求与会话失效降级
- [x] 4.3 为用户中心页面与最近播放子页面补充或更新 UI 测试，覆盖快捷入口、主页仅显示自己的歌单、游客态/已登录态分支与局部错误重试
- [x] 4.4 运行与本次改动相关的构建/测试命令，验证 app 模块在页面改版后可编译、测试通过
