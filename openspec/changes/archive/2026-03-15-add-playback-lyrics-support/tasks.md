## 1. 歌词数据与存储

- [x] 1.1 新增歌词远端数据源与 Repository，接入 `/lyric?id=<songId>`，并明确使用无需登录的请求配置
- [x] 1.2 实现歌词解析与领域模型映射，统一产出加载成功、暂无歌词和加载失败所需的数据结构
- [x] 1.3 新增本地歌词存储组件，支持按 `songId` 读写歌词并在写入后执行最多 100 首的淘汰策略
- [x] 1.4 在 `AppContainer` 中完成歌词仓库与本地存储的注入接线

## 2. 播放器状态与歌词同步

- [x] 2.1 扩展 `PlayerUiState` 与相关模型，补齐顶栏歌曲/歌词页切换、歌词加载态、当前行歌词摘要和完整歌词展示所需状态
- [x] 2.2 在 `PlayerViewModel` 中接通歌曲切换后的歌词读取、重试、本地命中优先、短稳定窗口远端拉取，以及切歌时旧歌词状态重置
- [x] 2.3 基于现有 `displayedSeekMs` 推导当前命中歌词行，并同步更新信息区摘要与完整歌词高亮
- [x] 2.4 在 app 侧补一份可复用的 display metadata 投影，统一产出“当前歌词摘要标题 + 歌名 - 歌手副标题”，供首页 minibar 与 `MediaSession` 共用
- [x] 2.5 为 `playback-contract`、`playback-client`、`playback-service` 接通 display metadata 投影命令与原始歌曲 metadata 保底字段，保证系统媒体入口可按歌词时间刷新标题且不反向污染 app 业务态

## 3. 播放页歌词体验

- [x] 3.1 更新 `PlayerScreen` 信息区，用当前时间对应歌词替换现有“歌词待补充”占位，并补齐无歌词空态
- [x] 3.2 在播放页顶栏增加更克制的“歌曲 / 歌词”切换，并把歌词页改成顶栏下方的完整沉浸页面：不显示歌名/歌手头部、不显示进度区/播控区、不再套卡片或边框，同时保留加载中、错误重试和高亮展示
- [x] 3.3 确保歌曲页与完整歌词页的横滑切换只作用于主内容区域；歌曲页进度拖动手势保持独立，歌词页上下边距与歌曲页封面/播控的视觉节奏对齐，小屏下不发生遮挡或抖动
- [x] 3.4 收敛完整歌词页当前命中行的视觉强调，保留高亮与自动滚动，但避免过大的字号跳变或突兀放大
- [x] 3.5 根据用户确认的参考稿继续收歌曲页前台层级：保持现有按钮样式不变，以布局重排为主，改成细指示线顶栏 tab、外壳包裹的封面层次、标题区右侧收藏，以及更弱化的辅助操作行

## 4. 首页 minibar 收口

- [x] 4.1 收紧 `MainShellScreen` 中 minibar 的整体高度、封面尺寸、间距和文字样式
- [x] 4.2 保持主体点击、横滑切歌、播放/暂停按钮和播放列表入口的现有交互语义不变
- [x] 4.3 校正紧凑布局下的长标题跑马灯、歌手信息和右侧控件区排布，避免压缩后重叠
- [x] 4.4 让 minibar 上行展示当前歌词摘要、下行展示“歌名 - 歌手”，并为需要滚动的长文案保持稳定跑马灯效果

## 5. 验证

- [x] 5.1 为歌词仓库、歌词解析与本地 100 首淘汰策略补充单元测试
- [x] 5.2 为播放页歌词摘要、完整歌词页切换/横滑、无框沉浸布局、无额外歌名歌手头部、自动滚动高亮、上下留白节奏，以及切歌/seek 同步补充 Robolectric 或 ViewModel 测试
- [x] 5.3 为首页 minibar 紧凑布局和既有交互行为补充回归测试
- [x] 5.4 运行 `./gradlew :app:testDebugUnitTest`、`./gradlew :playback-service:testDebugUnitTest` 和 `./gradlew :app:assembleDebug`
- [x] 5.5 为 minibar 的歌词标题展示、`MediaSession` 的动态标题/副标题投影，以及歌词高亮收敛后的页面表现补充回归测试
