## 1. 首页迷你播放条 UI 骨架

- [x] 1.1 将 `HomePlayEntryCard` 重构为胶囊式首页迷你播放条组件，收敛为“左侧封面 / 中间歌曲信息 / 右侧组合控件区”的单行布局
- [x] 1.2 基于现有 `PlayerUiState` 提取首页迷你播放条所需的轻量展示数据，优先消费运行时 `MusicInfo` / 播放快照已经投影到 `currentTrackTitle`、`currentTrackArtist`、`currentCoverUrl` 的字段，并在缺字段时回退到当前播放页的文件名解析逻辑与占位封面承载
- [x] 1.3 实现右侧“环形进度包裹播放/暂停按钮 + 邻接列表按钮”的组合控件区，确保小屏宽度下文案省略、控件尺寸和整体胶囊高度保持稳定
- [x] 1.4 为首页迷你播放条的长歌名补齐单行跑马灯承载，确保标题过长时不挤压封面、歌手信息和右侧控件区

## 2. Home 域交互与壳层接线

- [x] 2.1 在首页概览态接入新的迷你播放条主体点击行为，保持点击条目主体时仍通过现有 `HomeSurfaceMode` 进入 `PLAYER_EXPANDED`
- [x] 2.2 为首页迷你播放条补齐局部交互回调，使右侧播放/暂停按钮独立响应且不触发进入播放页
- [x] 2.3 为首页迷你播放条补齐列表入口交互，并将相关事件接到现有播放器状态/回调而不是新增旁路状态源
- [x] 2.4 为首页迷你播放条中间歌曲展示区补齐左右横滑切歌手势，左滑切到下一首、右滑切到上一首，并与主体点击进播放页保持清晰边界

## 3. 播放列表共享承载与状态收口

- [x] 3.1 将 `PlaylistBottomSheet` 的挂载位置从 `PlayerScreen` 内部提升到 Home 壳层共享位置，让首页概览态和播放展开态复用同一份列表面板
- [x] 3.2 调整首页迷你播放条、播放展开页和共享列表面板之间的状态收集与回调接线，确保 `showPlaylistSheet`、选择列表项、删除列表项和关闭面板行为不回退
- [x] 3.3 保持首页返回、播放展开页返回和底部 Tab 显隐行为稳定，避免引入新的层级抖动或遮罩冲突

## 4. 验证与回归

- [x] 4.1 补充首页迷你播放条的 UI / 状态验证，覆盖胶囊式布局、占位封面、标题/歌手展示与右侧组合控件区渲染
- [x] 4.2 补充交互验证，覆盖“点击条目主体进入播放页”“点击播放/暂停按钮仅切换播放状态”“点击列表按钮打开播放列表面板”三条路径
- [x] 4.3 补充歌曲展示区横滑切歌交互验证，覆盖“左滑下一首”“右滑上一首”“未达到阈值或无相邻曲目时仅回弹不切歌”三条路径
- [x] 4.4 补充长歌名展示验证，覆盖“标题过长时以单行跑马灯承载且迷你播放条布局不塌”路径
- [x] 4.5 运行 `./gradlew :playback-service:testDebugUnitTest`、`./gradlew :app:testDebugUnitTest` 与 `./gradlew :app:assembleDebug`，确认首页迷你播放条改造未破坏现有构建与播放器能力
