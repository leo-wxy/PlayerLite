## 1. 页面结构与主视觉

- [x] 1.1 把播放器展开页收成单条真实顶栏，统一承载返回、标题和分享按钮
- [x] 1.2 调整播放页为顶部正方形封面 + 深色基底 / 轻模糊封面铺底 / 深色遮罩的混合背景 + 底部信息区分层布局
- [x] 1.3 为封面取色链路增加 `allowHardware(false)` 与不可读位图安全回退，确保取色失败只回退不崩溃
- [x] 1.4 在当前曲目拥有稳定 `artistId` 时，让播放页歌手名支持直接跳转歌手详情

## 2. 进度区与播控区

- [x] 2.1 把默认 Slider 收口成更接近 progressbar 的细轨道样式，并同步调整底部间距，让进度区与播控区整体更靠下
- [x] 2.2 放大四个侧边播控按钮的图标与点击区，并让中间主播放按钮保持“明显更大但比例协调”的主次关系，同时为它们提供稳定背景
- [x] 2.3 保证小屏 / 窄屏设备上的播控区不重叠、不中断沉浸式布局

## 3. 状态提示与唤起行为

- [x] 3.1 把 buffering / 状态提示固定到封面右下角，避免底部内容区抖动
- [x] 3.2 buffering / preparing 时保持关键播控可用，不再把整排播控统一禁用
- [x] 3.3 让 MediaSession 内容入口直接回到播放器展开页

## 4. 验证

- [x] 4.1 为顶栏对齐、正方形封面、混合背景层、播控区布局、buffering 可操作性、主按钮主次关系、歌手名跳转和 MediaSession 唤起补充测试
- [x] 4.2 为封面取色的 `HARDWARE bitmap` / 不可读位图安全回退补充测试
- [x] 4.3 运行 `./gradlew :playback-service:testDebugUnitTest`、`./gradlew :app:testDebugUnitTest`、`./gradlew :app:assembleDebug`
